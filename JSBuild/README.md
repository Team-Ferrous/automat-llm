# CYBEL (aka automat-llm)

Mobile AI Assistant

## How To Run

To run this demo  you need to have Electron installed. Then run `electron run` or `electron build` in the root directory. Preferably the keys should be their own environment variables.
